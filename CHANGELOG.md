# 版本记录

## Beta 0.5.1（2026-09-19）—— 重做启动入口

### 新增

- **`停止.bat`**：一键停止平台，不需要再去任务管理器找进程
- **`创建桌面快捷方式.bat`**：在桌面生成「灾情分析平台」图标，
  以后双击桌面图标即可启动，不必每次翻文件夹
- **`serve --autorun`**：启动前先预加载示例数据并跑完整流程，
  页面一打开就是完整结果，演示时不必当着评委的面点两个按钮

### 启动器重做

- **先等端口就绪再开浏览器**：原先是先开浏览器再启服务，浏览器经常抢跑拿到错误页
- **已在运行时不再报错**：检测到平台已运行就直接开浏览器，重复双击不会出现端口冲突
- **服务在独立的最小化窗口中运行**，启动器窗口用完即关，不再需要一直挂着
- **就绪检测区分三种结果**：正常就绪 / 服务起来了但预分析没出结果（给出提示）/ 根本没起来
  （给出排查指引）。失败路径的等待时间从 2 分钟降到 45 秒

### 修掉两个真问题

1. **预分析静默失败**：把 `_run` 的逻辑抽到 `Session` 时漏了 `settings` 属性
   （原来 `self` 是 Handler，有 `settings` 类属性），一启动就抛
   `AttributeError`，被兜底逻辑吞掉后平台照常启动、页面却空空如也。
   现在 `settings` 显式传入 Session
2. **两个实例能同时监听同一端口**：`HTTPServer` 默认开启 `SO_REUSEADDR`，
   Windows 下允许多个进程绑定同一端口，结果是新旧实例同时监听、请求被随机分发，
   出现「明明启动成功却读不到数据」的怪现象。现在改为独占绑定，
   第二个实例会明确报端口占用并退出（退出码 3）

## Beta 0.5.0（2026-09-19）—— 警示视觉体系

配色从政务蓝改为应急警示色系，并加入应急场景的视觉语言。

### 视觉主题

- **主色改为警示橙红**：以 #c2410c 为主色、#d97706 为辅助色，成功/警告/危险语义色同步校准，
  浅色与深色两套主题都做了暖色重设
- **页头改为深红褐渐变**（#4a1508 → #7d2a12 → #b8471a）
- **新增警示带**：页头下方一条黄黑斜纹（45° 重复渐变），这是应急场景通用标识语言
- **新增"监测中"状态标识**：标题旁的红色脉冲圆点 + 胶囊标签，带呼吸动效
  （遵循 `prefers-reduced-motion`）
- 步骤指示器、指标卡色条、选项卡下划线、章节标题色条统一改为橙→红渐变
- 页面底色改为暖中性色，卡片投影也改用暖色阴影

### 热力图与图表配色

- **分级着色图与热力图由蓝色改为红色**：`geo_palette` 改为
  `#ffe8d6 → #fdc9a9 → #fb9a72 → #f4623a → #d92d1c → #9e0f10` 的红色分级刻度，
  数值越高越红，符合"损失越重越醒目"的直觉
- **主色板整体转暖**：以红橙金为主，保留青、蓝两个冷色用于多系列折线图的区分度
  （九条系列如果全用暖色会难以分辨）
- 静态图（matplotlib）与交互图（ECharts）共用同一套配色配置，
  改 `config/chart_rules.json` 即可整体换肤

### 说明

以上只涉及样式与配色，功能、数据流、元素 id 均未改动，9 项端到端自检保持通过。

## Beta 0.4.1（2026-09-17）—— 会话历史与删除

此前「智能问答」的回答只存在于当前页面，刷新即丢失，也就谈不上删除。
这一版先把会话留存下来，再提供删除能力。

### 新增

- **会话历史面板**（智能问答页右侧）：记录每次提问、回答、分析计划、所用模型与时间，
  最新在前，最多保留 50 条；选项卡同步显示记录数徽标
- **删除单条**：每条记录右上角有删除按钮，点击即移除
- **清空全部**：面板右上角「清空历史」按钮
- **点击历史可回看**：点击任意记录，在左侧还原当时的计划与回答；当前选中的记录会高亮

### 设计说明

- **记录只存在本机浏览器的 localStorage，不经过服务器**。这个平台没有登录体系，
  本地存储既能让「删除」真正生效（删掉的字节确实从磁盘上消失），也避免把提问内容留在服务端。
  面板底部有明确提示文字
- **清空全部采用两步确认，不用原生 confirm**：第一次点击按钮变为「确认清空？」，
  4 秒内再点一次才执行，超时自动复原。原生弹窗会阻塞页面，且与整体视觉不统一
- **删除单条不弹确认框**：单条记录误删代价很低，加确认反而拖慢操作
- **历史只保留文字回答，不保存图表数据**。地图数据的单条记录就有上百个区域，
  存进浏览器存储会迅速撑爆配额；回看时会提示「图表未随记录保存，重新提问即可再次生成」
- 回答中的 Markdown 加粗标记现在渲染为真正的粗体（先做 HTML 转义再渲染，
  避免模型输出被当作标签执行）

## Beta 0.4.0（2026-09-17）—— 界面视觉升级

**只改样式层与呈现结构，功能、数据流、元素 id、渲染逻辑全部未变**，
9 项端到端自检保持通过。

### 视觉

- **页头改为深蓝渐变**（深色 #10243a → #16334f → 深青），配盾形标识
  （盾牌 + 等高线意象，纯内联 SVG，不依赖任何图标库）
- **重新定义色彩体系**：以政务蓝为主色、青绿为辅助色，补齐成功/警告/危险语义色，
  浅色与深色两套主题分别校准
- **新增主题切换**：跟随系统 → 浅色 → 深色 三态循环，选择记入 localStorage
  （答辩现场光线不同时可切换）
- **选项卡加图标**，选中态用渐变下划线；按钮全部带图标并统一为圆角胶囊
- **处理阶段由文字标签改为步骤指示器**：完成态为渐变实心圆 + 对勾，未完成态为序号
- **指标卡重做**：左侧渐变色条、更大的数值、说明行独立成行
- **卡片与表格**：卡片圆角与投影分级，表格斑马纹 + 悬停高亮 + 吸顶表头
- **空状态**改用图标 + 虚线边框的引导样式
- 面板切换有轻微淡入上移动效，并遵循 `prefers-reduced-motion`

### 顺带修好的三个可读性问题

1. **图表文字在深色主题下对比度不足**：ECharts 默认文字色是深灰，
   现在按当前主题动态覆盖标题、坐标轴、图例、提示框与地图配色，切换主题后自动重绘
2. **多系列图例挤成多行压住坐标轴**：图例改为单行可滚动（`1/3 ▶`）
3. **环形图标签被裁切**：占比移入扇区内显示，名称交给图例

### 一个布局陷阱

标题容器此前允许无限收缩（`min-width: 0`），窗口偏窄时会被工具栏挤成
**一列一个字**。已改为 `min-width: 360px` 并让文字容器撑满剩余宽度。

## Beta 0.3.1（2026-09-17）—— 界面改为选项卡

### 本次变更

- **单页长滚动改为选项卡式多页面**，7 个选项卡：概览 / 数据接入与清洗 / 多维分析 /
  可视化图表 / 分析报告 / 智能问答 / 规则与配置
- 选项卡带计数徽标：待确认数据质量问题数、分析计划数、图表数
- **新增「概览」页**：处理阶段状态、8 个核心指标卡、智能体执行轨迹
- **新增「规则与配置」页**：只读展示全部外置配置——分析维度（标注自定义维度）、
  损失指标、图型选择规则、报告章节骨架。答辩时可以现场说明「规则可配置」
- 当前选项卡记入 localStorage，刷新后停留在原页；也支持 `#charts` 这样的地址定位
- **图表改为按需渲染**：ECharts 在隐藏容器里初始化会拿到 0 宽度导致画不出来，
  现在切到「可视化图表」页才初始化，并带重新计算尺寸的逻辑
- 执行轨迹记入 sessionStorage，刷新页面后不丢失

## Beta 0.3.0（2026-09-17）—— 补齐空间分布图

赛项要求里的「空间分布类图表（热力图、分级着色图）」此前是用排序条形图代替的，
这一版真正补上了。至此四类图表要求全部满足。

### 本次新增

- **行政区划边界数据**：`data/geo/sichuan_city.json`（21 个市州）、
  `data/geo/sichuan_county.json`（183 个区县），含各区县中心点坐标。
  来源为公开的行政区划边界数据，已随包分发，**离线可用**
- **两个新的图型**
  - `choropleth` 分级着色图：按行政区划边界对指标值分级上色
  - `spatial_heatmap` 热力图：以县级行政中心为点位、按指标值渲染热力强度
- **新增两张标准图表**：`分县（区）直接经济损失空间分布`、`分县（区）直接经济损失热力图`
  （标准图表由 8 张增加到 10 张）
- **新增智能体工具 `render_heatmap`**：可直接按维度与指标生成空间分布图，
  工具总数由 6 个增加到 7 个
- **静态图与交互图双输出**：静态图用 matplotlib 直接绘制行政区划多边形，
  交互图交给 ECharts（前端按需向 `/api/geo` 拉取并注册地图）
- 规则引擎新增 `requires_geo` 条件：安装了边界文件时区域维度优先使用分级着色图，
  未安装时自动降级为排序条形图，不会报错

### 处理细节

- **区分「无数据」与「零损失」**：没有灾情记录的区县不着色，显示为浅灰；
  热力图则完全不绘制该区域的热力点
- **重名行政区**：四川省内「市中区」同时存在于内江市与乐山市。地图要素按名称匹配，
  两地会被同一数值着色。彻底解决需要新增「市+县」组合维度，已记入已知限制
- 数值分级与配色写在 `config/chart_rules.json` 的 `geo_palette`

## Beta 0.2.1（2026-09-17）

### 本次新增

- **大模型接入配置化**：新增 `config/llm.local.json` 作为密钥文件（已在 `.gitignore` 中忽略），
  优先级高于环境变量；未配置时自动回退到离线规则模型，不影响任何已有功能
- **`llm-setup` 命令**：交互式写入密钥，使用 getpass 读取，密钥不回显、不进入命令历史
- **`llm-check` 命令**：打印脱敏后的配置状态，并发一条最短请求测试连通性
- **`打包Beta.bat` / `scripts/build_release.ps1`**：一键打包发版，自动排除运行产物与密钥文件，
  打包后会校验压缩包内不含 `llm.local.json`，不通过则中止发布
- 逐项检查文档：`使用说明.md` 增加大模型接入章节
- **修复筛选条件取值问题**：接入真实模型后发现，模型会给出「近五年」这类描述性筛选值，
  底层匹配不到任何记录，导致分析结果为空。现在会做两件事：
  规划提示词中附带各维度真实出现过的候选值，要求原样选取；无法匹配的筛选条件被忽略并在
  回答开头说明，不会再返回空结果
- **修复报告正文三处缺陷**（在网页端通读报告时发现）：
  1. 损失结构段落出现 `占 57.84%%` 的重复百分号；
  2. 结论段落句子之间出现「。；」、结尾出现「。。」；
  3. **区域占比分母错误**：县级排名只取前 20 条，占比却按这 20 条之和计算，
     得出 22.42%，真实值应为 7.81%。现在一律取未被截断的 `totals` 作为分母
- **问答答案补充指标单位**：此前模型回答「808166.14」后附注「数据中未注明单位」，
  现在事实数据中带上指标单位，回答直接给出「808166.14万元」

### 说明

接入真实模型后，模型只用于两处：把自然语言问题翻译成分析计划、基于事实数据回答提问。
报告中的全部数字仍由代码注入，不经过模型。规划结果若包含非法维度或指标会被自动丢弃并回退。

## Beta 0.2.0（2026-09-16）—— 首个可运行版本

这是第一个可以交给队友和评委直接运行的版本。五个模块全部是可运行实现，不是占位代码。

### 本次新增

- `启动.bat`：一键启动，自动检查 Python 与依赖库，自动准备示例数据并打开浏览器
- `自检.bat`：双击即可用模拟数据验证环境，7 项检查全 PASS 表示正常
- `使用说明.md`：面向非技术使用者的操作手册
- 版本号与 `--version` 命令

### 已具备的功能

**数据接入与清洗**

- 多文件、多工作表批量导入，自动探测标题行，兼容 `.xlsx` 与 `.csv`
- 字段级比对报告：已映射字段、未识别列、缺失的必需字段
- 十步清洗：文本归一化、多格式日期解析、单位换算（亿元/万元/元、万亩/亩/公顷）、
  缺失值填充、按灾种分组 IQR 异常值盖帽、业务上下限截断、行政区划层级校验、去重
- 每一次数据修改都写入清洗日志：改了什么、依据什么、影响多少行

**多维多层级分析**

- 单一分组聚合入口，灾种 / 时段（年季月日）/ 区域（省市县乡）/ 损失结构全部为维度参数
- 8 个标准分析计划一次跑通
- 2 个自定义维度：汛期与非汛期、人均损失分档

**可视化**

- 图型由规则表决定，共 6 种：折线图、面积图、柱状图、排序条形图、环形图、矩阵热力图
- 每张图同时输出静态 PNG 和 ECharts 交互配置，离线时自动退回静态图

**报告生成**

- 五章正文 + 附录数据质量说明，含表格与图表
- Word / PDF / Markdown 三种格式导出，PDF 使用内置中文字体，不依赖字体文件
- 所有数值从分析结果对象注入，不经过大模型

**智能体层**

- 6 个工具：读模板、数据体检、清洗、聚合分析、出图、写报告
- 状态机主循环 + 结果校验（记录数单调收敛、合计值非负）
- 支持自然语言问答：意图理解 → 分析计划 → 调用工具 → 校验

**界面**

- 本地网页界面，7 个板块与工作流一一对应
- 支持上传文件、一键分析、自定义维度出图、报告下载、自然语言提问

### 环境

- Python 3.10+（已在 3.14.5 上验证）
- 依赖：pandas、numpy、matplotlib、openpyxl、python-docx、reportlab
- 大模型：默认离线规则模型，无需 API Key；配置环境变量后可切换到任意 OpenAI 兼容接口

### 已知限制

1. **空间分布目前是排序条形图**，不是赛项要求的"分级着色图"。接入行政区划 GeoJSON 后
   可在 `charts.py` 增加 `choropleth` 分支。
2. **行政区划参照表是演示用最小集**，未覆盖的县只做代码前缀校验，不做名称比对。
3. **"单次事件"按记录条数近似**。真实数据若要按灾害事件归并，需要补充事件 ID 字段。
4. **跨字段一致性校验尚未实现**（如死亡+失踪 ≤ 受灾人口、分项损失之和 ≤ 直接经济损失）。
5. **报告没有目录、图表编号和公文样式模板**。
6. **本地服务为单机演示定位**，无鉴权、无并发控制，会话状态保存在内存中，重启即重置。
7. **表格字段规范基于赛题描述的占位设计**，拿到组委会正式模板后需要在
   `config/schema.json` 的 `aliases` 中补齐字段映射。

### 下一步计划

- 接入四川省县级行政区划 GeoJSON，补上分级着色图
- 补充跨字段逻辑校验规则
- 替换为完整的行政区划代码参照表
- 报告增加目录、图表自动编号与公文体模板
- 接入真实大模型并补充规划准确率评估
