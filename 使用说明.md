# 使用说明（Beta 0.2.0）

面向「安全与应急创新」赛项四川省减灾中心企业命题：
**自然灾害灾情数据自动化综合分析辅助决策支撑平台**。

不需要懂编程。按下面的步骤操作即可。

## 一、准备

电脑上需要装 **Python 3.10 或更高版本**（[下载地址](https://www.python.org/downloads/)）。
安装时**务必勾选 "Add Python to PATH"**，否则启动脚本找不到它。

如果只是你自己这台机器，第一次已经装好了，直接跳到第二步。

## 二、启动（双击一个文件）

**建议先做一次**：双击 **`创建桌面快捷方式.bat`**，桌面会多出一个「灾情分析平台」图标。
以后直接双击桌面图标就行，不用再翻文件夹。

双击 **`启动.bat`**（或桌面图标），启动器会依次：

1. 检查 Python 与依赖库（缺了会自动联网安装，首次约 1–3 分钟）
2. 检查平台是否已经在运行（已经在运行就直接开浏览器，不会报端口冲突）
3. 在最小化窗口中启动服务
4. **预加载示例数据并跑完整分析**，然后打开浏览器

浏览器打开后是这个地址：`http://127.0.0.1:8000`，
**页面打开时结果已经在了**，不需要再点按钮。

启动器窗口会自动关闭（服务在另一个最小化窗口里运行）。

### 停止平台

双击 **`停止.bat`** 即可，或者手动关掉那个最小化窗口。

## 三、跑一次完整分析

界面上只有两个按钮要点：

1. 点 **「加载示例数据」**——顶部会显示两个示例文件名
2. 点 **「一键分析」**——等 3～5 秒，五个阶段灯依次点亮

界面顶部有 7 个选项卡，点击切换：

| 选项卡 | 看什么 |
| --- | --- |
| 概览 | 处理阶段状态、核心指标卡、智能体执行轨迹 |
| 数据接入与清洗 | 字段匹配率、未识别列、缺失的必需字段；每条清洗动作改了什么、影响多少行；待确认问题 |
| 多维分析 | 10 个分析计划的合计值（可展开明细），以及自定义维度出图 |
| 可视化图表 | 10 张图，含分县分级着色图与热力图；鼠标悬停有数值提示 |
| 分析报告 | 下载 Word / PDF / Markdown / 清洗日志 / 清洗后数据，并预览正文 |
| 智能问答 | 用自然语言提问，例如"近五年洪涝灾害直接经济损失趋势"。右侧「会话历史」可回看往次提问，每条记录右上角的 × 可删除单条；「清空历史」需点两次确认。记录只存在本机浏览器，删除后不可恢复 |
| 规则与配置 | 只读展示全部外置规则：维度、指标、图型规则、报告骨架 |

## 四、换成自己的数据

1. 点 **「上传 Excel」**（可按住 Ctrl 多选，支持 `.xlsx` 和 `.csv`）
2. 再点 **「一键分析」**
3. **重点看第一节**：如果有「未识别列」或「缺失的必需字段」，说明表头和平台的字段规范对不上

对不上的处理办法：把真实表头补进 `config/schema.json` 里的 `aliases`，保存后刷新页面重跑。

例如你的表头叫「灾害直接经济损失」，就加一行：

```json
"灾害直接经济损失": "direct_economic_loss"
```

## 五、结果文件在哪

全部输出在 `output/` 目录下：

- `output/reports/` —— Word、PDF、Markdown 报告和清洗日志
- `output/charts/` —— 8 张静态图表（PNG）
- `output/processed/cleaned_records.csv` —— 清洗后的数据

界面第六节也有对应的下载按钮。

## 六、常见问题

**双击 `启动.bat` 一闪而过**
说明 Python 没装或没加到 PATH。重新安装 Python 并勾选 "Add Python to PATH"。

**浏览器提示"无法访问此站点"**
服务没起来，或者黑色窗口被关掉了。重新双击 `启动.bat` 即可。

**提示端口被占用**
换一个端口启动：在黑窗口里按 Ctrl+C 停掉，然后手动执行
`python -m disaster_agent.cli serve --port 8001`，浏览器访问 `http://127.0.0.1:8001`。

**图表显示成静态图片，鼠标悬停没有提示**
说明浏览器没加载到图表库（离线环境常见），程序自动退回了静态图，不影响功能。

**想确认环境是否正常**
双击 **`自检.bat`**，会用模拟数据跑一遍全流程并检查每一项输出，全部显示 PASS 就没问题。

## 七、给开发者

命令行入口：

```bash
python run_demo.py                          # 一键跑通并打印执行轨迹
python -m disaster_agent.cli run --agent    # 跑通并输出清洗日志
python -m disaster_agent.cli ask "问题"      # 自然语言问答
python -m disaster_agent.cli catalog        # 查看可用维度与指标
python -m disaster_agent.cli --version      # 查看版本
python tests/test_pipeline.py               # 端到端自检
```

架构说明、赛项要求对照表、二次开发指引见 `README.md`。

## 八、接入大模型（可选）

不配置也能用——平台默认走离线规则模型，所有功能正常。想让"智能问答"用真实大模型时：

```bash
python -m disaster_agent.cli llm-setup     # 按提示输入接口地址、模型名和密钥
python -m disaster_agent.cli llm-check     # 验证配置与连通性
```

密钥会写入 `config/llm.local.json`，这个文件已经加进 `.gitignore`，**不会被打进分发包**。
`llm-check` 显示的是脱敏后的密钥（形如 `sk-ab****cdef`），可以核对有没有填错。

也可以手动复制 `config/llm.config.example.json` 为 `config/llm.local.json` 再填写，内容形如：

```json
{
  "api_key": "sk-你的密钥",
  "base_url": "https://服务商地址/v1",
  "model": "模型名"
}
```

任何 OpenAI 兼容接口都可以，换服务商只要改 `base_url` 和 `model` 两行，代码不用动。

> **安全提醒**：不要把这个文件连同项目一起发给别人；不要在聊天、文档或代码里粘贴密钥。
> 用 `打包Beta.bat` 打包时会自动排除并校验，可以放心分发。
